<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- تنبيهات قرب السوبرماركت (اختياري، opt-in من NearbyDealsScreen) — مش مطلوبة لباقي التطبيق -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- الاهتزاز عند التسبيح ونجاح مسح الكاميرا. صلاحية عادية (بتتمنح وقت التثبيت،
         مفيش نافذة طلب) — كانت ناقصة، فكل نداء vibrate() كان بيرمي SecurityException
         ويتبلع في الـ try/catch: الاهتزاز مكانش شغال في أي مكان في التطبيق. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- camera.any بدون required=false: التطبيق بيشتغل على أجهزة/تابلتس بدون كاميرا
         (كل ميزات الكاميرا optional في وقت التشغيل) — كان بيفلتر هواتف من Play Console -->
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.location.gps" android:required="false" />
    <uses-feature android:name="android.hardware.location.network" android:required="false" />

    <application
        android:name=".ZadApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:localeConfig="@xml/locales_config"
        android:theme="@style/Theme.MyApplication">

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-4433736715872551~7866252380" />

        <!--
          Persists the in-app language choice on API < 33. AppCompat's
          setApplicationLocales() only writes the selection to disk when this service is
          declared with autoStoreLocales=true; without it the locale lives in memory for
          the session and the app silently reverts to the system language on next launch.
          On API 33+ the framework stores it and this declaration is inert.
        -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="zad" android:host="invite" />
                <data android:scheme="zad" android:host="family" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="zad" />
            </intent-filter>
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- /rewards — زر تليجرام لما رصيد المحادثة يخلص. لازم https مش
                     zad://: تليجرام بيقبل http/https/tg:// بس في أزرار الروابط
                     وبيرفض أي مخطط مخصص، فالزر نفسه ماكانش هيتبعت أصلاً. -->
                <data android:scheme="https" android:host="zad.app" android:pathPrefix="/rewards" />
                <data android:scheme="http" android:host="zad.app" android:pathPrefix="/rewards" />
                <data android:scheme="https" android:host="zad.app" android:pathPrefix="/invite" />
                <data android:scheme="https" android:host="zad.app" android:pathPrefix="/family" />
                <data android:scheme="http" android:host="zad.app" android:pathPrefix="/invite" />
                <data android:scheme="http" android:host="zad.app" android:pathPrefix="/family" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CrashActivity"
            android:exported="false"
            android:theme="@style/Theme.MyApplication" />

        <!-- Unified Bank Notification Listener (replaces 3 old listeners) -->
        <service
            android:name=".services.UnifiedBankListener"
            android:label="Zad Bank Listener"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <service
            android:name=".services.ChatNotificationService"
            android:exported="false" />

        <!-- FCM — الوعي اللحظي للأيدجنت (إشعارات استباقية حتى والتطبيق مقفول) -->
        <service
            android:name=".services.ZadFirebaseMessagingService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>

        <!-- "Hey Zad" wake word — استماع دائم، foreground service بإذن المايك -->
        <service
            android:name=".voice.HeyZadWakeService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Pharmacy dose reminders: exact-time alarm trigger + notification actions -->
        <receiver
            android:name=".receivers.PharmacyReminderReceiver"
            android:exported="false" />

        <!-- Re-schedule pharmacy alarms after reboot (AlarmManager alarms don't survive it) -->
        <receiver
            android:name=".receivers.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- زرار "اسمع زاد" على إشعارات لحظات الصوت (VoiceMomentSpeaker) -->
        <receiver
            android:name=".voice.VoiceMomentSpeaker$ListenReceiver"
            android:exported="false" />

        <!-- تنبيهات قرب السوبرماركت: geofence transitions (GroceryGeofenceManager بيسجلها) -->
        <receiver
            android:name=".receivers.GeofenceBroadcastReceiver"
            android:exported="false" />

        <!-- Transaction Widget -->
        <receiver
            android:name=".widgets.TransactionWidget"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.zad.REFRESH_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/transaction_widget_info" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
