# SESSION_HANDOFF — اقرأه هو الأول

آخر تحديث: ٢٠٢٦-٠٩-١٤. لو جلسة كلود جديدة فتحت على الريبو ده، ابدأ من هنا قبل أي حاجة
تانية — بما فيها `docs/agent/SESSION_2026_09_06_device_test.md` والباقي. الملف ده بيقول
فين إحنا واقفين بالظبط، مش تلخيص — كل رقم فيه اتقاس مش اتفترض.

## ٢٠٢٦-٠٩-١٤ (مساءً): مميزات «الترند» + اللهجة + ملف العميل + فحص المفاتيح

| كومِت | إيه |
|---|---|
| `c04b850f` | «فين راحت فلوسي؟» كل جمعة ٥ العصر بتوقيت العميل — النبرة (فخر/عتاب) من الأرقام (008) |
| `27581404` | وضع الطوارئ «أنا مفلس»: `zad_broke_mode` (009)، مصروف اليوم، وقف اقتراحات الشراء، وصفات من المخزون بس |
| `a9675b5a` | تحدي ٣٠ يوم توفير (010): تقييم الصبح سيرفر-سايد، العداد مايتعدّلش من التطبيق (grant على status بس) |
| `4562e7d6` | دخول منطقة تسوق + اتفاق توفير ⇒ فويس «افتكر اتفاقنا» مرة في اليوم |
| `cf1e5693` | زاد بتهزر على الفاتورة (على الموبايل بس، مرتين في اليوم، OCR منضف كبيانات) |
| `5e3bdac0` `ed83931b` | صور تتشير من غير مبالغ: «أسبوعي مع زاد» وترتيب بستان العيلة |
| `f06986ce` | «أرخص سعر حواليك» (011) + بوست قناة يومي — **وإصلاح: بلاغ السعر كان مابيتحفظش** (timestamp رقم + EGP ثابتة) |
| `2b62ebbb` | رمضان/العيد بتقويم أم القرى + فويس قبل المغرب بـ٢٠ دقيقة من مكان البيت على الموبايل |
| `2cd7302a` `14618110` | CI بعد كل نشر: أسماء الأسرار + فحص حي لكل مفتاح؛ مفتاح exchangerate بقى يغذّي أسعار العملات |
| `e870bb57` | اللهجة: كل لهجة تعليمتها مكتوبة بيها بأمثلة ومنع الفصحى (`_shared/dialect.ts`) |
| `872cffb6` `c732063d` | ملف العميل `zad_customer_profile` (012) + أداة `update_customer_profile` + كارت في «زاد عارف عني إيه» |

**فحص المفاتيح الحي (CI run بعد `872cffb6`):** LocationIQ ✅ (10 أماكن حوالين القاهرة)، Pexels ✅،
تليجرام ✅، exchangerate ✅، Gemini ٥ مفاتيح ✅. **USDA = 403** (المفتاح مرفوض/مش متفعّل)، **Groq المفتاح
الأول = 401** (التاني شغال)، ElevenLabs مش متحط، `TELEGRAM_COMMUNITY_CHAT_ID` مش متحط (البوست اليومي
بيتخطى). ملاحظة: `supabase secrets list` في اللوج ماطلعش `ZAD_API_KEY_*` بس الفحص الحي بيأكد إنهم
موجودين وشغالين — الفحص الحي هو المرجع.

**السبب الجذري لـ«الإيجنت مايعرفنيش»:** `buildSnapshot` كان بيقرا `gender` ومايحطوش، ومابيقراش الاسم،
وذاكرة الشات أقرب ١٢ ملاحظة للرسالة بس. دلوقتي كارت `customer` في كل سناب شوت، وملاحظات الهوية
(`salary_plan`، `household_profile`...) دايماً في السياق.

## ٢٠٢٦-٠٩-١٤ (بعد الظهر): شخصية صوت زاد + لحظات الصوت + المواعيد + الأماكن (١٥ كومِت)

طلب المستخدم: صوت بنت لطيفة بمشاعر كاملة (بتهزر/تعاتب/تزعل/تعيط/تفرح) بلهجة كل عميل وفي كل
قناة، تعرف إنه صحي، مواعيد بالكلام، تعرف خرج فين وصرف إيه، الدوا اللي فات يتقال بصوتها،
والباقي كتابي. قراراته: الحركات على السيرفر بخصوصية، زاد تتكلم لوحدها، مشاعر كاملة.

| كومِت | إيه |
|---|---|
| `7a32073e` | اختيار البلد كان بيتكرر: `commitMarketSelection` بيكتب `MarketPrefs` قبل التنقل |
| `91dca104` | `_shared/zadVoice.ts`: ٩ مشاعر + لهجة ١٩ سوق + `MOMENT_EMOTIONS` — نفس الشخصية في المكالمة وتليجرام والموبايل |
| `1b639920` | طابور `zad_voice_moments` (ميجريشن 003): الجرعة نص → بعد ساعة عتاب بصوتها → تاني مرة زعل |
| `381dda8f` | حقل شات العيلة كان بيتزق مرتين فوق الناف بار (`consumeWindowInsets`)؛ احتياجات أمازون الوهمية اتشالت |
| `b8395364` | المكالمة الحية بتوصل العقل كله (`ask_zad_brain` → agent_turn بكل الأدوات) — إضافة/حذف بالصوت بيتنفّذ |
| `f449235e` `17de64fe` | `zad_appointments` (004) + أدوات العقل + شاشة «مواعيدي» + تذكير بصوتها قبل الميعاد |
| `dc9d98d6` | «صباح الخير» لما يفتح القفل الصبح (بعد ≥٣ ساعات قفل، مرة في اليوم) + احتياطي الساعة ١٠ (005)؛ «سبّحت النهارده؟» بصوتها |
| `0605cdd9` | بانر ربط تليجرام في الرئيسية وتحذير صريح إن من غيره مفيش تقارير/فويس |
| `ae29b465` | `zad_place_visits` (006، **من غير إحداثيات** — البيت على الموبايل بس) + «رجعت! روحت فين وصرفت إيه» |
| `fd8b784f` | CLAUDE.md: الـ٣١٣ نداء كانوا شبكات كرون مش دخول شاشة |
| `7093c7e8` | لوحة قوة الصرف: العنوان والشريط كانوا 1.35:1 — خلفية ثابتة وألوان مقاسة (`WcagContrastTest`) |
| `8bc96f42` | الرئيسية: كارت المخزن الفاضي كان صف فاضي، وملخص العقل الفاشل كان «بيحلل» للأبد |
| `907786a1` | `zad_place_reminders` (007): «فكّريني لما أروح الصيدلية» — بتتقال بصوتها عند store_arrival، مرة واحدة |

**النشر:** ميجريشنز 003–006 اتطبّقت من CI (`Applying migration 20260914006000_place_visits.sql` ←
`Finished supabase db push`، run 34840831875) وzad-brain اتنشر. `gh run list/view` **شغّال** في
الكونتينر ده (عكس الملاحظة القديمة إن مفيش أداة بتقرا Actions) — القراءة بس.

**خطة التشخيص (الأرتيفاكت) خلصت:** كل بند يا متنفّذ يا مستني جهاز/كرون. الأهداف اتزرعت بعد التسجيل
(`8d6717b9`)، والذاكرة مربوطة بالماسح (`20260913190000`)، وتقييم الوصفات موصّل (`rate_recipe`).

**محتاج جهاز (مش متحقَّق):** صباح الخير عند فتح القفل، تعلّم مكان البيت (عينة ٣ الفجر)، فويس اللحظات
على الموبايل (`SpeakWorker`)، تذكير المكان عند محل حقيقي، المكالمة الحية لآخرها. **Play Console:**
نطاق `ACCESS_BACKGROUND_LOCATION` اتوسّع (البيت + تذكيرات الأماكن) — إقرار الموقع في الخلفية لازم
يتحدّث (`docs/agent/PLAY_CONSOLE_BACKGROUND_LOCATION.md`).

## ٢٠٢٦-٠٩-١٤: الصوت الموحّد + إصلاحات واجهة من لقطات جهاز حقيقي (٧ كومِتات، كلها منشورة)

**السبب الجذري لـ"المساعد الصوتي مش بيرد":** مش كود. حساب الجهاز (`abek52278@gmail.com`)
كان `voice_left = 0`، و`zad-voice-live` بيرد على الـupgrade بـ402، و**بوابة Supabase بتحوّل أي
رد مش 101 على طلب WebSocket لـ502 من غير body** (اتقاس بـcurl) — فالتطبيق بيقول "تعذّر الاتصال"
ومفيش لوج. كمان: **لوجات Supabase مابتسجّلش طلبات WebSocket خالص**، فـ"صفر طلبات في
function_edge_logs" مش دليل إن التطبيق مابعتش. والباقة المجانية مابتعيدش تعبئة `voice_left`
أصلاً (zad_entitlement_refresh بيعبّي chat/scan بس).

| كومِت | إيه |
|---|---|
| `6387d8f0` | `voice_monthly = -1` لكل الباقات (قرار المستخدم: الصوت مفتوح للكل لحد خطوة الإعلانات) |
| `45484dd2` | الرفض بقى upgrade + إغلاق بكود 4401/4402/4503/4502 وسبب؛ **ثغرة أمنية اتقفلت**: anon key + هيدر `x-user-id` كانوا بينفّذوا أدوات zad-brain باسم أي مستخدم. متحقَّق على الإنتاج: anon → `close 4401 "unauthorized"` |
| `0d40578d` | شبكة كل الأقسام (`ZadSectionsGrid`، مصدر واحد مع شيت المزيد)، تليجرام تحت الشبكة، شيف زاد مابقاش يطبخ "ماء إيلان" (`isCookableIngredient`)، الوصفة بتظهر فورًا، شريط أمازون full-bleed، الكورة العايمة في الرئيسية بس |
| `2b21e82a` | مسار صوت واحد: `ZadLiveVoiceSession` اتمسح، مفيش زرار أوضاع، بديل دور-بدور تلقائي، barge-in حقيقي (`BARGE_IN_LEVEL`)، تشغيل على خيط مستقل، الشخصية بتوصل المكالمة (`?voice=`) |
| `3d77d259` | `CompanionOrb` اتعاد رسمه كائن 3D لطيف (صور مرجعية)، وبقى الأفاتار الوحيد (ZadVoicePet اتمسح) |
| `03a8770c` | تنبيهات تليجرام الحرجة بفويس بصوت Aoede: ميزانية ٩٠/١٠٠٪، insight حرج، `listener_gap_alert`، `spending_ahead`. **متحقَّق حي**: `[voiceAlert] delivered 755086 bytes PCM as voice note` |
| `a6f2179c` | تباين: textTertiary الفاتح كان 2.49:1 → 4.78 (`WcagContrastTest`)؛ ألوان الأقسام اتنقلت لـ`ZadSectionAccent` في ui/theme؛ كارت لوحة الأسعار كان بيقص عنوانه؛ حدود خانات "ضيف دوا" |

**⚠️ درس الجلسة:** `0d40578d` كسر `Build Debug APK` في CI لحد `a6f2179c` — `checkNoRawColorsInScreens`
(معلّق على `lintDebug`) بيرفض أي `Color(0x…)` جديد في ui/screens وui/components، والبناء
المحلي (`assembleDebug`) مابيشغّلوش. شغّل `./gradlew checkNoRawColorsInScreens` قبل أي دفعة واجهة.

**مش متحقَّق منه ومحتاج جهاز:** المكالمة الحية بـJWT مستخدم حقيقي لحد صوت جيميناي (اللي اتحقق:
البوابة + المصادقة + أكواد الإغلاق)؛ قيمة `BARGE_IN_LEVEL` (0.25) على سماعة خارجية؛ شكل الفويس
MP3 في تليجرام (البوت قبله بـ`ok`). **حساب `abek52278` لسه على باقة `pro` من غير انتهاء** (اتعمل
قبل قرار فتح الصوت للكل) — لو مش مقصود يرجع `free`.

## تحديث ٢٠٢٦-٠٩-١٣ (متأخر): النشر خلص بنجاح — الحظر اتفك

`seam1010x-lab/zad-app` (الريبو الصح — شوف تصحيح الريبو تحت) فعّل Actions وضاف
`SUPABASE_ACCESS_TOKEN`/`SUPABASE_DB_PASSWORD`. تشغيلة `Edge Functions` بعد كده
**نجحت (Success)**. اتحقق مباشرة من الداتابيز بعد النجاح (`mcp__supabase__list_migrations`
+ `execute_sql`)، مش من مجرد لون الـCI الأخضر:

- **الـ٨ ميجريشنز كلها وصلت**: `20260912210000` لحد `20260913003000` ظاهرين في
  `supabase_migrations.schema_migrations`.
- **الـ٨ جداول المرمّمة موجودة فعلاً** (`family_groups`, `family_members`,
  `app_notifications`, `chat_messages`, `family_chores`, `family_goals`,
  `family_tasbiha`, `shared_grocery_list`) + `zad_waste_log` + `sent_budget_alerts`.
- **الماسح الاستباقي بقى نضيف**: التشغيلة اللي بعد الديبلوي (`13:07:00`) رجّعت
  `net._http_response` بـ`status_code: 200` و`{"ok":true}` — **مفيش `42P01` خالص**،
  عكس الأعطال اليومية اللي كانت موثّقة قبل كده.
- **`agent-tasks-processor` بيقرا من الـVault فعليًا**: `cron.job.command` بقى فيه
  `public.zad_cron_secret('zad_agent_tasks_cron_secret')` بدل النص الصريح، والتشغيلات
  بعد الديبلوي رجّعت `200 {"ok":true,"processed":0,"failed":0}` (صفر = طابور فاضي،
  مش خطأ مصادقة — لو كان فيه 401 كان هيبان هنا).
- **مفيش `insight` جديد لسه** (آخر واحد لسه ٢٠٢٦-٠٨-٣٠) — ده **متوقّع ومش عطل**: الفانكشن
  بقى بيشتغل نضيف، بس مفيش حاجة تستاهل تنبيه للمستخدمين القليلين دول في الساعة اللي
  فحصناها. `zad_proactive_silence_check` (الحارس الجديد من نفس الديبلوي) هو اللي
  هيتابع الاتجاه ده — لو عدّى ٣ أيام تانية من غير `insight` **مع** التشغيلات دي شغالة
  نضيفة، وقتها يبقى فيه حاجة تستاهل نظر.

**تحديث تاني (بعد كده بشوية):** تدوير `X-Agent-Tasks-Cron-Secret` (قسم ٤) **خلص فعليًا**
وكشف باج حقيقي في المقارنة نفسها (مقارنة سرّ مش متسامحة مع مسافة بيضاء) — اتصلح بكومِت
`50577832` ومحقَّق منه بتشغيلة كرون حقيقية رجعت `200`. **مفيش خطوة معلّقة من الخطة
الأصلية دلوقتي.** التفاصيل الكاملة في قسم ٤.

## تصحيح ٢٠٢٦-٠٩-١٣: الريبو الصح

`seam1010x-lab/zad-app` هو المستودع الخاص بالمستخدم، و`abek52278-ops/zad-app` مجرد
الأصل اللي هو `fork` منه. فروكات جيت هاب بتيجي بالـActions مقفولة افتراضيًا ومالهاش
وراثة أسرار — ده اللي كان يفسّر ليه `seam1010x-lab` معندهاش ولا تشغيلة CI، لحد ما
المستخدم فعّلها وضاف السرّين يدويًا (٢٠٢٦-٠٩-١٣). كل أمر `gh`/فحص لازم يستخدم
`seam1010x-lab/zad-app` من هنا وطالع.

مفيش أداة في بيئة التطوير دي (لا `gh`، لا الـGitHub MCP) بتقدر تشغّل أو تراقب Actions
runs مباشرة — توكن الجلسة App-scoped، و`403 Resource not accessible by integration`
على أي endpoint بتاع Actions permissions/secrets/runs حتى لو الـREST بيقول
`admin: true` على الريبو نفسه، وGitHub MCP نفسه ملوش أدوات Actions خالص. التحقق
الموثوق الوحيد كان مباشرة من الداتابيز عبر Supabase MCP (فوق)، مش من حالة الـrun.

## ٠. لو مش هتقرا غير فقرة واحدة

**النشر خلص. ١٣+٣ كومِت (الأصلية + إصلاحات الريبو/الديبلوي) على `main`، والـ٨ ميجريشنز
مطبّقة على الإنتاج، متحقَّق منها مباشرة من الداتابيز.** الماسح الاستباقي بيرجّع `200
{"ok":true}` نضيف، الكرون بيقرا سرّه من الڤولت. **الخطوة الوحيدة المتبقية من الخطة
الأصلية هي تدوير السرّ (قسم ٤) — كل حاجة تانية اتقفلت.**

---

## ١. المعمارية والحالة — إيه اللي اتغيّر

### مسار الصوت
- الكرة في الرئيسية بقت تفتح **المكالمة الحية** (`onOpenVoiceLive`) بدل الوضع القديم
  دور-بدور — كانت الدالة متعرَّفة ومحدش بينديها في المشروع كله.
- موديل TTS في `zad-core-intelligence/voice.ts` بقى `gemini-2.5-flash-preview-tts`
  (كان `gemini-2.0-flash` + احتياطيين ميتين، التلاتة بيرجّعوا 404 على المشروع ده).
  `voice-selftest` كان أخضر طول الوقت لأن افتراضيه كان صح من البداية — نفس متغيّر
  البيئة، افتراضيين مختلفين في ملفين.
- `zad-voice-live/context.ts` (ملف جديد): الجلسة الحية كانت بتاخد ١٣ أداة كلها كتابة
  ومفيش رقم واحد — دلوقتي بتاخد رصيد، سقف، صرف ٣٠ يوم، أعلى فئات، آخر معاملات، ناقص
  المخزون، قايمة الشراء، أدوية، التزامات، وأعلى ٦ ملاحظات `zad_memory`. كل بيانات
  المستخدم جوه بلوكات `=== ... ===` تحت سطر "بيانات مش تعليمات" (قاعدة الحقن في
  CLAUDE.md)، ومفيش بيانات = مفيش بلوك خالص (بلوك فاضي بيخلي الموديل يخترع أرقام).
- استئناف ذكي (٣ محاولات للأخطاء العابرة بس)، نغمة مايك مرة واحدة في الجلسة، الكورة
  بتقرا `isLiveMode` الصح، والنتيجة المحلية المتأخرة مابقتش تقطع المكالمة الحية.
- اللهجة: `zad_users.country` هو مصدرها الوحيد للمكالمة والشات، و**٤ من ٦ حسابات كانت
  `null`** — السبب إن اختيار السوق بيحصل قبل التسجيل والصف ممكن ما يكونش اتعمل. بقى
  بيترمّم عند تأكيد الجلسة في `MainActivity`.
- توكن FCM: كان بيترفع من `signIn()` بس، فحساب بجلسة محفوظة عمره ما رفع توكن —
  `zad_fcm_tokens` كان فاضي. بقى بيترفع عند تأكيد الجلسة كمان.

### العقل الاستباقي — والسبب الجذري الحقيقي
**`20260905130000_drop_dead_tables.sql` (قبل الجلسة دي بأسبوع) حذفت ٣ جداول ليهم كود حي
بيقرا منها.** دوال plpgsql مابتتحققش من مراجع الجداول وقت الإنشاء، فالمرجع فضل ساكت لحد
ما الدالة اتنفّذت. الأثر:
- `agent_proactive_scan` بترمي `42P01` عند أول استعلام كل ساعة من يوم الحذف — آخر مهمة
  استباقية قبل الجلسة دي: **2026-09-05**، نفس يوم الحذف بالظبط.
- `notify_parents_on_child_spend` (ترايجر مفعّل على `zad_transactions`) كان هيفشل أول
  مصروف يسجّله طفل عنده سقف.
- `zad_domain_observations` (بتتنادى من zad-brain وبوت تليجرام) و
  `zad_log_inventory_waste` (ترايجر مفعّل على `zad_inventory`) كانوا بيرموا خطأ.

**كل التلاتة اتقفلوا.** `sent_budget_alerts` و`zad_waste_log` رجعوا (ليهم كُتّاب حية)،
`user_alert_snooze` فضل محذوف (مالوش كاتب أصلاً — القُرّاء بس هما اللي اتحدّثوا).
**تصحيح لتوثيق سابق في نفس الجلسة:** أول تشخيص قال "٦ دوال مكسورة" وحطّ `zad_brain_stats`
معاهم — ده كان **إنذار كاذب** (الفحص طابق اسم متغيّر `v_active_nodes` مش جدول؛ في
بوستجرس `\b` بالريجيكس معناها backspace مش حدّ كلمة). العدد الحقيقي كان ٥، مش ٦.

فوق كل ده: تنبيهان (`spending_ahead`, `med_followup`) كانوا بيقفلوا الإدخال بـ
`on conflict on constraint` باسم قيد **مالوش وجود** — بترمي `42704` كل مرة، بيتبلعها
`exception when others`. اتصلحوا بنفس شكل الأربع دوال الشغالة (`on conflict (cols)
where ...`).

**جديد:** `_agent_spend_forecast_for_user` — توقّع أسبوعي (متوسط الصرف المحسوب +
الالتزامات المستحقة خلال ٧ أيام مقابل الرصيد). و`zad_proactive_silence_check` — حارس
جديد بيتنبه لو عدّى ٣ أيام من غير أي مهمة استباقية ولا رؤية، حتى لو صفر أخطاء (الحارس
القديم بيقيس نسبة الفشل بس، وكان بيرجع "too few runs" كل ساعة لأن `zad_brain_runs`
نفسها كانت صفر).

### سكيما وأمان
- **٨ جداول** (`family_groups`, `family_members`, `app_notifications`, `chat_messages`,
  `family_chores`, `family_goals`, `family_tasbiha`, `shared_grocery_list`) التطبيق
  بيقرا ويكتب فيهم ومالهمش `create table` في أي ميجريشن — بيئة جديدة كانت هتطلّع تطبيق
  مكسور. اترجعوا كلقطة لحظية من الإنتاج (نفس الأعمدة/القيم الافتراضية/السياسات بالحرف).
  معاهم `get_my_family_ids()` — مستخدمة في ٣ ميجريشنز ومتعرّفة في ولا واحد.
- آخر سرّ كرون نص صريح (`agent-tasks-processor`) اتنقل نمطه للڤولت — **تفصيل مهم تحت
  في قسم ٤**، لأن ده الجزء اللي محتاج تسلسل دقيق.

### الواجهة
- الفرضية القديمة "٣ أنظمة ثيم" قديمة: `ZadV2 = ZadV3` (محدش بينده)، `ZadLuxe` مش
  بالِت (٦ أسماء بديلة). النظام الحي واحد: `Color.kt → ZadTheme → MaterialTheme`.
- `UrgentRecipeCard` (سطح ثابت + ألوان مش من العلامة) → `tertiaryContainer`/`tertiary`.
- الحالة الفاضية في الرئيسية → `ZadEmptyState` (كانت سطر نص أعزل).
- "عروض قريبة" → **"المتاجر والأسواق القريبة"** (بقرار المستخدم) — الشاشة كانت أمينة
  فعلاً بس الاسم بيوعد بعروض مش موجودة. العروض الحقيقية شغّالة في مكان تاني
  (`LiveDealsCard` عبر `fetch_live_deals`). وحدة المسافة ("م"/"كم") كانت مكتوبة في
  الكود — بقت من `strings.xml` في ٤ لغات.
- ملخص الأب لتليجرام: ترويسة الملف كانت بتوعد بنداء تليجرام من يوم ما اتكتبت، ومكانش
  فيه ولا نداء واحد. بقى بيبعت فعليًا عبر `?job=realtime_push`.

**التفاصيل الكاملة بالأرقام والقياسات:**
[`docs/agent/SESSION_2026_09_12_silent_failures.md`](./SESSION_2026_09_12_silent_failures.md).

---

## ٢. الكومِتات — بالترتيب

| كومِت | الموضوع |
|---|---|
| `e4e4de19` | فتح القناة الحية + إصلاح موديل TTS + استئناف ذكي + نغمة مرة واحدة |
| `e982a764` | سياق ومعرفة المكالمة الحية + ترميم لهجة الحساب + الجداول الـ٨ الناقصة |
| `5f3aac13` | إصلاح `ON CONFLICT` المكسور في تنبيهي الصرف والدوا |
| `2284f2cd` | تنبيه التوقّع الأسبوعي الجديد |
| `db5caee7` | إصلاح `UrgentRecipeCard` (سطح ثابت + ألوان مش من العلامة) |
| `040e4aa7` | اكتشاف وإصلاح فوضى حذف ٥ سبتمبر (٥ دوال، مش ٦) |
| `48cf12dc` | ترجيع `zad_waste_log` |
| `3eb685ba` | ملخص الأب بقى بيوصل تليجرام فعليًا |
| `d4a10356` | نقل آخر سرّ كرون نص صريح لنمط الڤولت (⚠️ تقوية مش تدوير — شوف قسم ٤) |
| `88e53109` | إصلاح باج ذاتي: ميجريشن التوقّع كانت هترجّع مرجع مكسور |
| `609e1ccc` | تسمية الشاشة + وحدة المسافة + الحالة الفاضية |
| `ba7ceb97` | ملف الجلسة التفصيلي |
| (هذا الملف) | `SESSION_HANDOFF.md` + تصحيح تعليق قديم في `20260913001000` |

كل التستات عدّت: `assembleDebug BUILD SUCCESSFUL` (آخر مرة `3m 18s`)، ٣٧ تست وحدة صفر
فشل، `deno test` على `zad-voice-live`/`zad-core-intelligence` كلها عدّت، `deno check`
نضيف على كل ملف اتلمس.

---

## ٣. حالة CI/CD الفعلية (متحقَّق منها الآن، ٢٠٢٦-٠٩-١٣)

```
gh run list --repo seam1010x-lab/zad-app --limit 2
```

| الووركفلو | النتيجة | التفصيل |
|---|---|---|
| **Edge Functions** | ❌ فشل في ١م١٢ث | `check` (deno check + tests) نجح في ٥٣ث. `deploy` وقع عند أول خطوة: `supabase link` → `Unauthorized`. **صفر ميجريشن اتطبّق، صفر فانكشن اتنشرت.** متحقَّق مباشرة من الداتابيز: `select version from supabase_migrations.schema_migrations where version >= '20260912000000'` رجع **صف واحد فاضي**. |
| **Build Debug APK** | ❌ فشل (شكليًا) | كل خطوة كود حقيقية نجحت: Unit tests ✓، Lint ✓، **Check dependency alignment (حارس Ktor/supabase) ✓**، Build debug APK ✓. الفشل الوحيد: `Failed to CreateArtifact: Artifact storage quota has been hit` — حصة تخزين GitHub Actions، **مالوش علاقة بالكود خالص**. |

**لازم يتصلّح قبل أي حاجة تانية:** `SUPABASE_ACCESS_TOKEN` في
`Settings → Secrets and variables → Actions` بمستودع `seam1010x-lab/zad-app`. محتاج
Personal Access Token جديد من لوحة تحكم Supabase. **مفيش أداة في بيئة التطوير دي بتضبط
سرّ مستودع** — ده لازم يتعمل من لوحة GitHub مباشرة أو `gh secret set` بصلاحية إدارية.

بعد ما يتصلّح: إعادة تشغيل الووركفلو الفاشل (`gh run rerun 34723494451 --repo
seam1010x-lab/zad-app`) أو دفعة كومِت جديد كافية تبدأ تشغيلة جديدة.

---

## ٤. تدوير `X-Agent-Tasks-Cron-Secret` — ✅ خلص (٢٠٢٦-٠٩-١٣)

**التدوير اتعمل ونجح، متحقَّق منه من تشغيلة كرون حقيقية (مش نداء يدوي بس):**
تشغيلة `agent-tasks-processor` الساعة `13:55:00` رجّعت `200 {"ok":true,"processed":0,"failed":0}`.

**اللي حصل بالترتيب:**
1. قيمة جديدة عشوائية (`gen_random_bytes(32)` جوه بوستجرس، ٦٤ حرف hex) اتحطت في
   `vault.zad_agent_tasks_cron_secret` عبر `vault.update_secret`.
2. أول محاولتين لضبط سرّ المشروع `ZAD_AGENT_TASKS_CRON_SECRET` من لوحة Supabase
   **فشلوا صامتين (`401`)** رغم تأكيد المستخدم إنه نسخ ولصق بدقة، وحتى بعد حذف
   السرّ وإعادة إنشائه من الصفر. مفيش أداة هنا تقدر تقرا سرّ الإيدج فانكشن بعد
   الحفظ (write-only فعلًا في الواجهة)، فمفيش طريقة تتأكد من غير تجربة.
3. **الحل:** بدّلنا فحص `process_agent_tasks` من `hasConfiguredSecret()` الصامتة
   لـ`secretMatches()` (`_shared/cronSecret.ts`) اللي بتسجّل طول+بصمة SHA-256 (مش
   القيمة نفسها) في اللوج لو فيه اختلاف. أول نداء بعد النشر كشف السبب فورًا:
   `received len=64 ... expected len=65 ... trimmed match=true` — يعني القيمة
   المحفوظة فيها حرف مسافة/سطر جديد زيادة واحد بس، ناتج عن نسخها من كتلة كود في
   الشات.
4. **الإصلاح الجذري (كومِت `50577832`):** `secretMatches()` بقت تتسامح مع مسافة
   بيضاء زيادة في أي طرف (بعد `.trim()` على الاتنين) بدل ما ترفضها — نفس المشكلة
   بالظبط حصلت مع `ZAD_FX_CRON_SECRET` يوم ٢٠٢٦-٠٩-٠٧ (ست محاولات وقتها). مفيش
   خسارة أمنية: الأسرار دي عشوائية وطويلة، والمقارنة أصلاً مش timing-safe ومش
   محتاجة تبقى. هذا الإصلاح بيغطي **كل** الدوال اللي بتستخدم `secretMatches`
   (parent-digest، zad-telegram-bot، zad-fx-refresh) + هاجّرنا فحصي W9
   (`run_proactive_scan`) و`nightly_dream_reflection` في zad-brain من
   `hasConfiguredSecret` لنفس الحارس المشترك.
5. **ملحوظة تشغيلية اتعلمناها:** نشر إيدج فانكشن على Supabase مش فوري/ذري عبر كل
   مناطق الـedge — شفنا فرق حقيقي بين نداء يدوي رجع `200` وتشغيلة كرون بعدها
   بدقائق لسه واخدة `401` من نسخة قديمة (`deployment_id` بينتهي بـ`_227` بدل
   `_228`)، قبل ما يستقر بالكامل خلال دقايق قليلة. **لا تعتبر أول `200` يدوي دليل
   نهائي — استنى تشغيلة كرون حقيقية واحدة على الأقل.**

**القرار المفتوح الوحيد اللي فضل** (لو حد عايز نظافة تجميلية، مش لازم فعليًا):
القيمة المحفوظة في `ZAD_AGENT_TASKS_CRON_SECRET` لسه فيها الـ٦٥ حرف (مع المسافة
الزيادة) — شغّالة ١٠٠٪ دلوقتي بفضل التسامح الجديد، مفيش داعي تتلمس تاني.

<details>
<summary>السياق التاريخي (قبل الإصلاح، محفوظ للمرجعية)</summary>

⚠️ **ده الجزء اللي أي جلسة جديدة لازم تقراه قبل ما تلمس السرّ ده.**

فيه **مخزنين منفصلين** لنفس القيمة، مش مخزن واحد:

1. **سرّ مشروع Edge Function**: `ZAD_AGENT_TASKS_CRON_SECRET` — متغيّر بيئة، `zad-brain`
   بتقراه بـ`Deno.env.get(...)` وبتقارنه بهيدر الطلب الوارد.
2. **Vault (داتابيز)**: صف باسم `zad_agent_tasks_cron_secret` — أنشأته الجلسة دي عبر
   `execute_sql` مباشر، القيمة فيه **نفس القيمة القديمة المحروقة** (اتنسخت من
   `cron.job.command` الحالي، مش قيمة جديدة). ده **تقوية مش تدوير** — الهدف كان بس
   إن القيمة تبقى في مكان واحد بدل نص صريح، تمهيدًا للتدوير الحقيقي.

الكرون (`cron.job`) لسه **بيقرا القيمة القديمة نص صريح من `command`** — ميجريشن
`20260913003000_agent_tasks_cron_secret_to_vault.sql` هي اللي بتعيد جدولته يقرا من
Vault بدل النص الصريح، **ومحتاجة تُنشر الأول** (نفس المشكلة في قسم ٣).

**لو السرّ اتدوّر دلوقتي (قبل النشر)، هيحصل الآتي بالحرف:**
1. تتغيّر قيمة `ZAD_AGENT_TASKS_CRON_SECRET` في Supabase → `zad-brain` بقى يستنى القيمة
   الجديدة.
2. `cron.job.command` **لسه فيه القيمة القديمة** (مالهاش أي علاقة بالميجريشن اللي مانشرتش).
3. كل تشغيلة `agent-tasks-processor` (كل ٥ دقايق) هتبعت القيمة القديمة → `401` صامت —
   **نفس شكل العطل بالظبط اللي الجلسة دي كلها كانت بتصلّحه.**

**الترتيب الصحيح الوحيد:**
1. يتصلّح `SUPABASE_ACCESS_TOKEN` (قسم ٣).
2. تُنشر الميجريشنز (CI أو `supabase db push` بعد ما التوكن يشتغل) — ده اللي بيخلّي
   `cron.job` يقرا من Vault.
3. **بعد كده بس**: تتولّد قيمة عشوائية جديدة (نفس أسلوب تدوير `ZAD_FX_CRON_SECRET`
   الموثّق في `SESSION_2026_09_06_device_test.md`: `gen_random_bytes` جوه بوستجرس، من
   غير ما القيمة تعدّي على أي جلسة chat)، تتحط في Vault (`vault.update_secret` — بيحدّث
   نفس الصف)، **وفي نفس الوقت** تتحط في سرّ مشروع `ZAD_AGENT_TASKS_CRON_SECRET` (خطوة
   لوحة تحكم يدوية — لا يوجد أداة هنا تضبط أسرار مشروع عن بعد).
4. يتأكد إن `agent-tasks-processor` بترجع `200` بعد أول تشغيلة تالية.

**لا تنفّذ الخطوة ٣ قبل ٢.** لو مستعجل ومحتاج التدوير قبل ما التوكن يتصلّح، الخيار
البديل الوحيد الآمن هو تحديث `cron.job` نفسه يدويًا (`cron.alter_job` أو
`cron.unschedule` + `cron.schedule` بقيمة الهيدر الجديدة نص صريح مؤقتًا) في نفس اللحظة
اللي بيتحدّث فيها سرّ المشروع — وده يرجّع نفس مشكلة "نص صريح" اللي الجلسة دي قفلتها،
فمش مستحسن إلا لو فعلاً مفيش وقت.

</details>

---

## ٥. قرارات مفتوحة (بقرار المستخدم صراحة: متأجّلة، مش هتوقف النشر)

- **الدوال الثلاث "المكسورة":** كانت مذكورة في تشخيص أوّلي كـ`zad_brain_stats`،
  `zad_domain_observations`، `zad_log_inventory_waste`. **تصحيح:** بنهاية نفس الجلسة،
  **الثلاثة اتقفلوا فعليًا** — `zad_brain_stats` طلعت إنذار كاذب (سليمة أصلاً)،
  والاتنين التانيين اتصلحوا بترجيع `zad_waste_log` (كومِت `48cf12dc`). **مفيش قرار
  منتج معلّق هنا فعليًا** — لو حد افتكر إن فيه ٣ دوال لسه مكسورة، ده تصوّر قديم من
  منتصف الجلسة قبل الإصلاح الكامل. اتأكد من قسم ١ فوق قبل ما تبني على الافتراض ده.
- **تسمية "المتاجر والأسواق القريبة":** اتنفّذت (كومِت `609e1ccc`)، مش معلّقة.
- **تدوير `X-Agent-Tasks-Cron-Secret`:** ✅ خلص (٢٠٢٦-٠٩-١٣)، شوف قسم ٤.

---

## ٦. ابدأ من هنا — خطوات فورية لأي جلسة كلود جديدة

1. اقرا الملف ده كامل (خلصت).
2. اقرا `docs/agent/SESSION_2026_09_12_silent_failures.md` للتفاصيل التقنية والقياسات.
3. `cd /workspaces/zad-app && git log --oneline -15 | cat` — تأكد إن آخر كومِت لسه
   `ba7ceb97` أو بعده (لو فيه كومِتات جديدة، ده معناه حد كمّل من هنا).
4. `gh run list --repo seam1010x-lab/zad-app --limit 3` — شوف هل `SUPABASE_ACCESS_TOKEN`
   اتصلّح ولا لسه. لو `deploy` بقى ناجح، روح مباشرة لقسم ٤ وابدأ تسلسل تدوير السرّ.
5. لو محتاج تبني محلي: راجع قسم "Heap" في `CLAUDE.md` — التوزيع المتحقَّق منه آخر مرة
   `-Xmx800m..900m` للجرادل + `-Xmx1900m..2000m` لكوتلن، `--max-workers=1`،
   `set -o pipefail` إجباري قبل أي `grep` على نتيجة البناء.
6. **ممنوع تفترض إن أي حاجة في قسم ١ لسه محتاجة تشخيص** — كل حاجة فيه متحقَّق منها
   بالقياس المباشر على الإنتاج أو بتجربة جافة موثّقة في الكومِت نفسه.

---

## ٧. المرحلة ٤ — شاشة صحة العقل الاستباقي (٢٠٢٦-٠٩-١٣)

**اتنفّذت:** `BrainHealthScreen` (أيقونة `MonitorHeart` في تاب عقل زاد). المنطق كله في
`data/BrainHealth.kt` (صافي، مغطّى بـ٢١ تست في `BrainHealthTest`)، والقراءة ٩ استعلامات
RLS عادية في `SupabaseRepo.getBrainHealth()` — **مفيش ميجريشن**.

**النسخة الأولى اتراجعت قبل الدفع بمراجعة ٣ agents على الداتابيز الحقيقية** — كانت
هتقول "تمام" وقت عطل حقيقي. الأسباب الجذرية (مهمة لأي حد يلمس المراقبة):
- ٢٥٧ من ٢٦٣ صف في `zad_brain_runs` هم `chat` — مش مقياس للعقل الاستباقي. الإشارة
  الحقيقية = `agent_tasks` بـ`kind <> 'reminder'`. عطل ٠٩-٠٦→٠٩-١٢ (صفر مهام) كان
  مخفي تمامًا ورا `cron.job_run_details` = 336/336 نجاح.
- مسار التحليل بيكتب الفشل `status:"queued"` مش `"failed"` — ونهائي.
- `zad_brain_queue` write-only: مفيش حاجة بتقراه أو بتعيد تنفيذه.

**فجوات السيرفر — ✅ اتقفلت نفس اليوم (٤ كومِتات، متحقَّق منها على الداتابيز بعد نشر CI):**
1. ✅ **`geofence_enter`** (`d4c3f99f`): `normalizeBrainTrigger()` في `shared.ts` بتحوّل أي
   trigger مش معروف لـ`event`، وخطأ إدراج `zad_brain_runs` بقى بيتسجّل بدل ما يتبلع.
2. ✅ **حارس الكلفة** (`c19882e4`، ميجريشن `20260913160000`): `started_at` بدل `created_at`
   المش موجود، والـcatch-all اتشال. بقى بيرجّع **1%** فعلي على الإنتاج (كان 0% دايمًا).
3. ✅ **الماسح الاستباقي** (`b42a0223`، `20260913161000`): الدوال المساعدة بترمي بدل ما
   تبلع، كل دالة في subtransaction لوحدها، الماسح بيرجّع jsonb، أي فشل = صف
   `proactive_scan_failure` + تنبيه أدمن يومي + HTTP 500 من الإندبوينت. ومفتاح
   `zad_brain_health_alerts` بقى `(alert_date, kind)` — كان بيكسر `proactive_silence` بصمت.
4. ✅ **أمان** (`71cc2dd7`، `20260913162000`): `_agent_spend_forecast_for_user` و
   `zad_proactive_silence_check` كانوا قابلين للنداء بالـanon key (security definer).

**التحقق بعد النشر (٢٠٢٦-٠٩-١٣ 16:35 UTC):** الميجريشنز الـ٣ في `schema_migrations`، و`zad-brain`
v229 من الـrunner. استدعاء يدوي بنفس أمر كرون `agent-proactive-scan-hourly` بالظبط (السر
من Vault جوه SQL، ماخرجش) → `net._http_response` id 11035: **HTTP 200**
`{"ok":true,"scanned":4,"failed":0,"failed_users":0,"skipped_orphans":2,"errors":[]}`، ولوج
Postgres: `agent_proactive_scan: {... "cost_pct": 1 ...}` — أول مرة الرقم مش صفر. إصلاح
`geofence_enter` متغطّي بتست + نفس الـbundle، بس ماتجرّبش حيًّا (محتاج JWT مستخدم حقيقي
ودخول نطاق فعلي).

**اتكشف أثناء الإصلاح ومحتاج قرار منك (مش متصلّح):**
- **٢ صفوف يتيمة في `zad_users`** (٦ صفوف مقابل ٤ في `auth.users`، ومفيش FK بينهم).
  `home_weekly_digest` كان بيقع عليهم كل ساعة بـ23503 من غير أي أثر. الماسح دلوقتي
  بيتخطّاهم وبيرجّع `skipped_orphans: 2`. **المسح نفسه قرار بيانات مستخدم** — مش من
  `delete-account` (بيمسح zad_users الأول)، غالبًا حسابات اتمسحت من الداشبورد. الحل
  الجذري: FK `zad_users.id → auth.users(id) on delete cascade` بعد تنضيف اليتامى.
- تحذير رصيد الشات مابيعرفش المستخدم مشترك ولا لأ (تحسين محتمل، مش عطل).
- `zad_proactive_silence_check` بيعتبر أي `zad_insights` إشارة حياة، فرؤى الشات ممكن
  تخبّي سكوت المهام الاستباقية على مستوى النظام (نفس فخ النسخة الأولى من الشاشة).

**✅ اتعمل:** حذف `ZadMindScreen.kt` (alias ميت لـ`ZadKnowledgeMapScreen`، صفر نقاط نداء) في كومِت منفصل.

---

## ٨. قرارات المستخدم + المرحلة ٣ (٢٠٢٦-٠٩-١٣، بعد الظهر)

**✅ اليتامى + FK** (`6d100922`، `20260913170000`): اتمسحوا بشرط وقت التطبيق (مش ids).
بياناتهم كانت معزولة (صفر عائلة فيها مستخدم حقيقي). `zad_users` 6→4، `family_groups`
24→14، و`zad_users.id → auth.users(id) on delete cascade`. **متحقَّق على الإنتاج بعد CI.**
ملاحظة: ٧ من ٥٤ جدول فيهم `user_id` لسه من غير FK على auth.users — نفس نوع اليتامى ممكن
يتكوّن فيهم لو حساب اتمسح من الداشبورد (مش شغل الماسح، مش عاجل).

**✅ فحص السكوت** (`4b5411c7`، `20260913171000`): `agent_tasks` بـ`kind <> 'reminder'` بس،
عتبة = max(36h، 1.5×وسيط الفجوة) زي `BrainHealth.kt`. على عطل 09-06: كان هيولّع 09-06
12:07 بدل 09-08. **حي على الإنتاج:** `{silent_hours:3, allowed_hours:36}`.

**المرحلة ٣ — الرئيسية (✅ خلصت، مدفوعة):**
- `50d3beaa`: ١٠ هيكسات بره العلامة ← توكنات. القياس كشف أعطال AA حقيقية مش شكل بس:
  بانر التنبيه 3.44:1 لايت / **2.55:1 دارك**، كهرماني التوقع 1.97:1، `#2196F3` 4.16:1.
- `14a1a451`: ٢٦ `fontSize` ثابت ← `Typography`. ولقطة Roborazzi كشفت «Zad Agent»
  **1.17:1** (مخفي تمامًا في اللايت) وكروت رؤى بيضا في الدارك — اتصلحوا.
- الـ١٤ هيكس و١٦ `fontSize` الباقيين كلهم في `KidsModeContent` — متعمَّد (CLAUDE.md).
- `AgentPanelContrastTest` بقى بيحرس ألوان اللوحة والبانر (5/5).

**المرحلة ٣ — شاشة العقل (✅ قرار المستخدم: البنفسجي هوية الذكاء الرسمية):** توكنات
`aiAccent`/`aiAccentEnd`/`onAiAccent`/`aiAccentSoft`/`aiNarrativeContainer`/`onAiNarrative`
في `ZadExtendedColors`. **اللايت بنفس القيم بالظبط** (مفيش تغيير بصري، ومحروس بتست)، والدارك
مقروء: صناديق سرد الذكاء بقت بنفسجي غامق بدل لافندر فاتح، والأبيض على بنفسجي الدارك (2.64:1)
بقى نص غامق (6.87). رقم صحة البيت كان ليموني `#84CC16` = **1.98:1** ← `scoreGood`. هيكسات
الشاشة 33→9. التحويل للزمردي لو القرار اتغيّر = قيم التوكنات بس. `AiIdentityContrastTest` 5/5.

**متسجّل ومش متصلّح (خارج القرار):** `ZadDarkPanel` (مكوّن مشترك) خلفيته `primaryDark`
**بتتقلب** (#143225 لايت، #3E8F68 دارك). مقياس `SpendingPowerGaugeCard` جوّاه بيلوّن الشريط
`primary` عند ≥60% = **1.35:1 في اللايت** (الشريط مش باين)، وفي الدارك الأرضية متوسطة الإضاءة
فكل ألوان المقياس ضعيفة. محتاج قرار على مستوى المكوّن (أرضية ثابتة داكنة زي لوحة الأيجنت؟)،
مش ترقيع لون واحد. ليموني `#84CC16` في نفس المقياس اتساب لنفس السبب.

---

## ٩. مرحلة ٠ (الصوت) — ✅ متنفّذة من ٢٠٢٦-٠٩-١٢، متتنفذش تاني من خطة التشخيص

الخطة (artifact بتاريخ 09-12) لسه كاتباها مفتوحة، بس الكود اتحقق سطر بسطر يوم 09-13: كل
البنود الخمسة موجودة، وأغلبها في `e4e4de19` (2026-09-12، "reach the live voice channel,
restore Gemini TTS"):
- الكورة وصف الترحيب ← `onOpenVoiceLive()` (`HomeScreen.kt:396,405`)، و`MainScreen.kt:486`
  بيحط `voiceSheetLiveMode = true`.
- `voice.ts:26` افتراضي `gemini-2.5-flash-preview-tts`، والسلسلة (`:102`) موديلات حية بس.
- `ZadVoiceBottomSheet`: `petAudioLevel` على `isLiveMode`، `submitVoiceTurn` بيرجع لو
  `isLiveMode`، المسار المحلي بيتقفل قبل بدء الحي، واستئناف صامت للأخطاء العابرة بسقف.

**متحقَّق على السيرفر (09-13 17:08 UTC):** `voice-selftest` ← `{"ok":true,
"model":"gemini-2.5-flash-preview-tts","audio_bytes":87744}`. الفانكشنين بيقروا نفس سر
`GEMINI_TTS_MODEL`، فتحذير الخطة «السر ممكن يغلب الكود بموديل ميت» **اتقفل**: القيمة الفعالة
في الإنتاج موديل شغّال.

**مش متحقَّق لسه (محتاج جهاز):** معيار الخطة نفسه — «اتصال واحد في لوجات `zad-voice-live`».
آخر ٢٤ ساعة: **صفر** اتصال بـ`zad-voice-live` وصفر بـ`zad-core-intelligence` — ومفيش أي
نشاط تطبيق أصلاً (آخر لفة شات 09-09). يعني محدش فتح التطبيق بالبيلد الجديد، مش إن الإصلاح
فاشل. الخطوة: تثبيت `app-debug.apk` على جهاز → الكورة → بعدها
`select ... from logs where source='function_edge_logs' and event_message ilike '%zad-voice-live%'`.

---

## ١٠. توصيل المبادرات لتليجرام + كرون الطابور — ✅ متحقَّق حي (٢٠٢٦-٠٩-١٣ 17:50 UTC)

**التوصيل** (`793d32ac`، `zad-brain` v230): نتايج المهام بـ`kind <> 'reminder'` بتروح
`zad-telegram-bot?job=realtime_push` بعنوان حسب النوع (`agentTaskNotice` في `shared.ts`) بدل
«زاد خلّص مهمة كنت طلبتها» للكل. قبلها النتيجة الاستباقية كانت بتقف في `app_notifications`
بس (FCM صفر توكن، ومفيش تليجرام في المسار).
- **اختبار حي بموافقة المستخدم:** مهمة `kind='delivery_test'` (id `f89dc384…`) لحساب الأدمن
  المربوط تليجرام → لفة 17:50 `processed:1` → لوج `→ telegram: delivered` 17:50:12. البوت
  بيرمي لو API تليجرام مارجّعش `ok: true`، فـ`delivered` = تليجرام قبل الرسالة فعلاً.
  صف المهمة + إشعار التطبيق بتوعها فضلوا في الداتابيز (مش ضارين، اسمهم واضح).

**كرون الطابور** (`94e47f72`، ميجريشن `20260913180000`): `agent-tasks-processor` بقى
`... WHERE EXISTS (pending و scheduled_for <= now())`. حي: 17:40 `1 row` (طابور فاضي، نداء
ضايع) → **17:45 `0 rows`** (من غير نداء) → 17:50 `1 row` (فيه مهمة، النداء عدّى). يوفّر ~٢٨٨
نداء/يوم.

**ملاحظة جانبية من اللوج:** `ZAD_AGENT_TASKS_CRON_SECRET: matched after trimming surrounding
whitespace` — السر المخزّن لسه فيه المسافة الزايدة (معروف من قسم ٤، غير ضار بفضل التسامح).
دلوقتي بيظهر بس لما الطابور فيه شغل.

**صفحة الخطة** اتحدّثت بقسم «حالة التنفيذ»: 14 متحقَّق · 4 مستني تحقق (جهاز/وقت) · 3 جزئي · 4 لسه.
الباقي بالترتيب: تجربة APK على جهاز (صوت حي + توكن FCM)، ثم الجزئي/اللسه (أهداف الأونبوردنج،
ربط الذاكرة بالماسح، مقياس `ZadDarkPanel`، ٣ حالات فاضية، زرار تقييم الوصفات، تصحيح CLAUDE.md).

---

## ١١. مرحلة ٢ / بند ١ — الذاكرة مربوطة بالماسح (✅ منشور، `02aee658`)

رفض العميل لمبادرة من تليجرام بقى بيوقف تكرارها. صف واحد في `zad_memory` بيحمل ملاحظة
مقروءة للموديل (`scope='proactive_dismissal'`) ومفتاح مهيكل للماسح (`subject_kind` +
`suppress_until`، ميجريشن `20260913190000`). المدد بقرار المستخدم: «مش مهم» 30 يوم بتتضاعف
(سقف 180)، «عرفت خلاص» 3، «الرقم غلط» 7، ومتابعة الدوا سقفها 3 أيام دايمًا.

**متحقَّق:** تجربتين جافتين (المدد، التضاعف، رفض الملكية/السبب/التذكير، والماسح `suppressed`
0 ← 5 ومابيعدّش كتم منتهي)؛ بعد النشر: الماسح الحي بيرجّع `suppressed:0`، و`realtime_push`
بـ`dismiss_task_id` لمستخدم مش مربوط `200 not linked`. `zad-brain` 233/0، البوت 89/0.

**✅ متحقَّق بضغطة حقيقية (20:50 UTC):** مهمة `delivery_test` (id `89b87d1d…`) وصلت تليجرام الأدمن
بالأزرار (`→ telegram: delivered` 20:50:13)، والمستخدم داس «مش مهم» → صف في `zad_memory` 20:50:51:
`subject_kind=delivery_test`، `evidence_count=1`، كتم **30 يوم** لحد 2026-10-13، والنوع
مكتوم فورًا بينما `spend_forecast` مش متأثر. مفيش أي خطأ في اللوج. الكتم على نوع اختبار الماسح
مابيولّدهوش، فسايبينه.

**خارج البند:** قايمة إشعارات التطبيق مالهاش زرار رفض (FCM صفر توكن، تليجرام هو القناة
الحية)، والكتم على مستوى النوع مش الفاتورة/الضمان بعينه.

---

## ١٢. مرحلة ٢ / بند ٢ — أول هدف حياة (✅ منشور، `8d6717b9` + `8ef90cfc`)

**تصحيح على الطلب:** شاشة `OnboardingScreen` **قبل تسجيل الدخول** (مفيش user_id، و`agent_goals.user_id`
عليه FK على auth.users)، فالزرع بقى **خطوة رابعة في كارت تفعيل الرئيسية** بعد التسجيل.

**ليه دالة حتمية مش الموديل:** `set_life_goal` عمره مااتنادى (صفر صف في agent_actions)، والتقدم
(`trg_agent_goal_progress`) بيتحرك بس لما مهمة مربوطة بالهدف تخلص — والعميل مايقدرش يكتب agent_tasks.
`zad_seed_life_goal()` (`20260913220000`، auth.uid بس، سقف ٥ أهداف نشطة، idempotent) بتسجّل الهدف +
مهمة متابعة أسبوعية مربوطة (`goal_review`). الواجهة: `LifeGoalPickerSheet` بـ٤ أهداف جاهزة + هدف بكلام العميل.

**ثغرتين في منفّذ المهام اتقفلوا عشان المتابعة تشتغل فعلاً:** إدراج المهمة المتكررة كان بيسقط `kind`
(فالمتابعة التانية كانت هتبقى reminder)؛ والكتم مكانش بيتقرا غير في الماسح — دلوقتي المبادرة المكتومة
بتتأجل لنهاية الكتم (`postponeForSuppression`) بدل ما تتلغي.

**حادثة CI:** أول تشغيلة على `8d6717b9` وقعت في "Schema contract" (الكود بقى بيقرا
`zad_memory.subject_kind`/`suppress_until` ومش في `_schema/schema.json`) والنشر اتخطى. الإصلاح
`8ef90cfc` اتدفع من worktree منفصل عشان مايشيلش commit جلسة `zad-app-5d` اللي لسه مستني موافقة.
**درس:** أي عمود جديد بيقراه TypeScript لازم يتضاف لـ`schema.json` في نفس الكومِت —
`cd supabase/functions/_schema && deno test --allow-all` قبل الدفع.

**متحقَّق على الإنتاج (21:32 UTC):** الميجريشن مطبّقة، الدالة موجودة (anon لأ، authenticated أيوه)،
`goal_review → متابعة الهدف`، `zad-brain` v233 ورد المنفّذ فيه `postponed`. تجربة جافة بـJWT محاكى
غطّت الرفض/السقف/الـidempotency/حلقة التقدم. `agent_goals` لسه صفر — **مستني أول اختيار من التطبيق**
(محتاج الـAPK الجديد على جهاز).

---

## ١٣. مرحلة ٢ / بند ٣ — قايمة النواقص لتليجرام عند دخول محل (✅ منشور، `1a5391e9`)

**اللي كان مكسور:** `GeofenceBroadcastReceiver` كان بيبعت `geofence_enter` لمسار التحليل — نداء
موديل كامل ورده بيرجع للجهاز وبيتجاهل. لا تليجرام ولا رؤية؛ تكلفة من غير أي أثر (والتعليق في الكود
كان بيدّعي إنه بيبعت نصيحة تليجرام).

**دلوقتي:** أكشن `store_arrival` في zad-brain (هوية JWT بس، من غير موديل): قايمة الشراء غير المشتراة +
المخزون الناقص (العميل + عيلته) + تلميح الجهاز للسوبرماركت/المول، والأدوية القربت للصيدلية؛ دمج
بمقارنة عربي، ١٠ أصناف + «و N كمان»، ولو مفيش ناقص مفيش رسالة. حراسات: كتم zad_memory (أزرار البند ١
شغالة)، محل واحد كل ٦ ساعات، ٣ رسايل في اليوم؛ كل رسالة صف agent_tasks (store_arrival, done). الإشعار
المحلي على الجهاز زي ما هو.

**إصلاح مصاحب (مهم للمراقبة):** فحص السكوت وشاشة الصحة كانوا بيعدّوا أي `kind <> 'reminder'` نشاط.
`store_arrival` و`delivery_test` مالهمش علاقة بالماسح — اتستبعدوا (ميجريشن `20260913230000` +
`NON_PROACTIVE_SIGNAL_KINDS` في BrainHealth.kt). **قياس حي:** قبل التعديل `silent_hours: 2` (صف
اختبار 20:46 كان مخبّي السكوت)، بعده `silent_hours: 9–10` (آخر مهمة استباقية حقيقية 13:07).

**متحقَّق (23:08 UTC):** الميجريشن مطبّقة، الاسم والاستبعاد حيين، واستدعاء `store_arrival` من غير JWT
بيرجّع رسالة مسار الوكيل الجديد (`agent actions require a user JWT`) = الراوت الجديد منشور.
zad-brain 245/0، schema contract 1/1، BrainHealthTest 22/22، assembleDebug ناجح.

**مش متحقَّق لسه:** تشغيلة حقيقية — محتاجة JWT مستخدم: تفعيل «تنبيهات الموقع» في التطبيق (إذن موقع
الخلفية) + دخول نطاق سوبرماركت، أو زيارة محل مسجّل. بعدها: `select * from agent_tasks where
kind='store_arrival'` + لوج `[store_arrival] ... → telegram:`.
